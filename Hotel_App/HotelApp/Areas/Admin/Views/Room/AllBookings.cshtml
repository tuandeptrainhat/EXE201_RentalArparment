@model List<HotelApp.Areas.Admin.ViewModels.BookingVM>

<div class="layout-demo-wrapper">
    <div class="card shadow-lg container p-5">
        <div class="container">
            <div class="titlepage">
                <h2>Danh sách đặt phòng</h2>
            </div>
            <table id="bookingsTable" class="table table-striped table-bordered">
                <thead class="table-dark">
                    <tr>
                        <th>STT</th>
                        <th>Khách hàng</th>
                        <th>Số điện thoại</th>
                        <th>Email</th>
                        <th>Trạng thái booking</th>
                        <th>Cọc</th>
                        <th>Ngày tạo</th>
                        <th>Hành động</th>
                    </tr>
                </thead>
                <tbody></tbody> <!-- Empty tbody for dynamic population -->
            </table>
        </div>
    </div>
</div>

@section PageScripts {
    <script src="~/loadData/AllBookings.js"></script>
}