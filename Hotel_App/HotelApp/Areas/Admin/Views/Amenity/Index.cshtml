@model List<Amenity>

<div class="layout-demo-wrapper">
    <div class="card shadow-lg container p-5">
        <div class="titlepage">
            <h2>Quản lý tiện ích</h2>
        </div>
        <button class="btn btn-primary mb-4" style="width: 200px;" onclick="location.href='@Url.Action("Create","Amenity")'">Thêm tiện ich</button>

        <table id="AmenityTableAdmin" class="table table-striped table-bordered mt-5 pt-5" style="width:100%">
            <thead class="table-dark">
                <tr>
                    <th>STT</th>
                    <th>Tên tiện ích</th>
                    <th>Mô tả</th>
                    <th>Chức năng</th>
                </tr>
            </thead>
        </table>
    </div>
</div>
<form>
    @Html.AntiForgeryToken() <!-- Tạo trường input chứa token -->
</form>
